import { Page } from '../shell/Page';
import { Question } from '../question/Question';

/** Paste a hash. The instrument's original first chapter, now one route among several. */
export function Verify() {
  return (
    <Page active="verify">
      <Question />
    </Page>
  );
}
